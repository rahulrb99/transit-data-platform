name: transit_data_platform
version: "1.0.0"
config-version: 2

profile: transit_data_platform

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  transit_data_platform:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +schema: marts
      +materialized: table

